import React from 'react';
import { render } from '@testing-library/react';
import Contacts from '../pages/About';

describe('Testa a página de Contatos:', () => {
  it('Renderiza um texto com a palavra `Cidades`', () => {});
});
